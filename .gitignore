# Vercel 静态构建输出（由 view_xy_server.py --static 生成）
public/

# 数据文件（DUO/CBS 抓取与计算产出）
*.gpkg

# 外部原始数据统一放在 raw_data/ 下，由 CLI fetch 写入，不进版本控制
raw_data/

# 纯生成输出：集中放在 generated/ 下，不进入版本控制
generated/

run_report_*.json

# 本地开发
.venv/
.pytest_cache/

# feature 中间信息
P*_*/

# Local Archive
archive/

# dev temp
refactor/
backlogs/
